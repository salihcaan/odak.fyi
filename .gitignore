CLAUDE.md
.DS_Store
node_modules/
dist/
.vite/
design-system/

# Vite env — only .env.example is checked in
.env
.env.local
.env.*.local

# Playwright MCP debug logs/snapshots
.playwright-mcp/

# /impeccable skill scratch (generated design-system snapshot)
.impeccable/

# Throwaway visual-verification screenshots dropped by Playwright MCP
# into the repo root when filename param lacks a directory prefix.
/*.png
